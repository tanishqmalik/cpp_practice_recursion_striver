// print subsequences whose sum is k
#include <iostream>
using  namespace std;

void sum(int index, vector <int>ds, int arr[],int limit){
    if(index>=limit){
        
    }
}

int main(){
    vector<int> ds;
    int arr[]={1,2,1};
    int index=0;
    int limit=3;

    sum(index,ds,arr,limit);
}